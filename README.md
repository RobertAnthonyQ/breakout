# Breakout

Monorepo de Breakout — organización estudiantil de innovación y emprendimiento (PUCP).

## Estructura

| Carpeta | Qué es |
|---|---|
| **`web/`** | Landing page `breakout.lat` — Next.js, deploy en Vercel (Root Directory = `web/`). Ver `web/README.md`. |
| **`org-hub/`** | Base de conocimiento de la organización: identidad, miembros, eventos, alianzas, plantillas, archivo, creaciones y tooling de contenido. Ver `org-hub/README.md`. |

Este repo es la fuente de verdad de movimiento lento. Las tareas diarias, reuniones y coordinación en tiempo real se manejan en Notion.

Los historiales de git de los dos repos previos (hub organizacional + landing de Robert) están preservados aquí (fusionados el 2026-05-16).
